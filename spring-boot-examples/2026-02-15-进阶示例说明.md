# Spring Boot 进阶示例说明（2026-02-15）

## 📦 新增示例总览

本次新增了3个Spring Boot进阶示例，涵盖企业级应用开发的核心功能：

| 序号 | 示例名称 | 运行命令 | 难度 | 核心内容 |
|-----|---------|---------|-----|---------|
| 9 | Transaction | `./run-app.sh transaction` | ⭐⭐⭐ | 事务管理 |
| 10 | Cache | `./run-app.sh cache` | ⭐⭐⭐ | 缓存应用 |
| 11 | Scheduled | `./run-app.sh scheduled` | ⭐⭐ | 定时任务 |

---

## 9️⃣ TransactionApplication - 事务管理

### 📝 学习目标
- 理解Spring事务管理机制
- 掌握@Transactional注解的使用
- 学习事务传播行为和隔离级别
- 了解事务回滚规则和最佳实践

### 🔑 核心知识点

#### 1. 声明式事务管理
```java
@Transactional  // 方法级事务
public void transfer(Long fromId, Long toId, BigDecimal amount) {
    // 扣款
    // 入账
    // 记录日志
    // 任何异常都会回滚
}
```

#### 2. 事务传播行为
- **REQUIRED**（默认）：有事务加入，无事务创建
- **REQUIRES_NEW**：总是创建新事务，挂起当前事务
- **SUPPORTS**：有事务加入，无事务非事务运行
- **NOT_SUPPORTED**：总是非事务运行，挂起当前事务
- **MANDATORY**：必须有事务，否则抛异常
- **NEVER**：禁止事务，有事务抛异常
- **NESTED**：嵌套事务（使用SevPoint）

#### 3. 事务配置属性
```java
@Transactional(
    propagation = Propagation.REQUIRED,  // 传播行为
    isolation = Isolation.DEFAULT,        // 隔离级别
    timeout = 5,                          // 超时（秒）
    readOnly = false,                     // 是否只读
    rollbackFor = Exception.class,        // 回滚异常
    noRollbackFor = IllegalArgumentException.class  // 不回滚异常
)
```

#### 4. 乐观锁（@Version）
```java
@Entity
class Account {
    @Version  // 乐观锁版本号
    private Long version;
}
```

### 🚀 运行方式
```bash
cd spring-boot-examples
./run-app.sh transaction
```

### 🧪 测试用例

#### 1. 查看所有账户
```bash
curl http://localhost:8080/api/accounts
```

#### 2. 成功转账（事务提交）
```bash
curl -X POST "http://localhost:8080/api/transfer?from=1&to=2&amount=100"
```

#### 3. 失败转账（事务回滚 - 余额不足）
```bash
curl -X POST "http://localhost:8080/api/transfer?from=1&to=2&amount=10000"
```

#### 4. 查看交易日志
```bash
curl http://localhost:8080/api/logs
```

#### 5. 测试事务传播行为
```bash
curl -X POST http://localhost:8080/api/test-propagation
```

#### 6. 测试独立事务（REQUIRES_NEW）
```bash
curl -X POST http://localhost:8080/api/test-requires-new
```

### 💡 学习要点

1. **事务基础**：理解ACID特性（原子性、一致性、隔离性、持久性）
2. **传播行为**：特别关注REQUIRED和REQUIRES_NEW的区别
3. **回滚规则**：默认只在RuntimeException和Error时回滚
4. **只读事务**：查询操作使用readOnly=true优化性能
5. **超时设置**：避免长时间占用数据库连接
6. **乐观锁**：使用@Version防止并发更新冲突

### 🔗 关联前面的学习
- [JdbcDemo.java](../../src/advanced/JdbcDemo.java) - JDBC事务基础
- [ConcurrencyDemo.java](../../src/advanced/ConcurrencyDemo.java) - 并发控制
- [JpaApplication](../src/main/java/com/example/jpa/JpaApplication.java) - JPA数据库操作

---

## 🔟 CacheApplication - 缓存应用

### 📝 学习目标
- 理解Spring Cache抽象层
- 掌握缓存注解的使用（@Cacheable、@CachePut、@CacheEvict）
- 学习缓存配置和管理
- 了解缓存策略和最佳实践

### 🔑 核心知识点

#### 1. 启用缓存
```java
@SpringBootApplication
@EnableCaching  // 启用缓存支持
public class CacheApplication {
    @Bean
    public CacheManager cacheManager() {
        return new ConcurrentMapCacheManager("users", "products");
    }
}
```

#### 2. 查询缓存（@Cacheable）
```java
@Cacheable(value = "users", key = "#id")
public User getUserById(Long id) {
    // 第一次查询：执行方法，结果放入缓存
    // 第二次查询：直接返回缓存数据
    return userDatabase.get(id);
}
```

#### 3. 更新缓存（@CachePut）
```java
@CachePut(value = "users", key = "#user.id")
public User updateUser(User user) {
    // 总是执行方法，并更新缓存
    return userDatabase.put(user.getId(), user);
}
```

#### 4. 删除缓存（@CacheEvict）
```java
@CacheEvict(value = "users", key = "#id")  // 删除单个
public void deleteUser(Long id) { ... }

@CacheEvict(value = "users", allEntries = true)  // 删除所有
public void clearCache() { ... }
```

#### 5. 条件缓存
```java
@Cacheable(
    value = "users",
    key = "#id",
    condition = "#result != null && #result.age > 18",  // 缓存条件
    unless = "#result == null"  // 排除条件
)
```

#### 6. 组合操作（@Caching）
```java
@Caching(
    cacheable = { @Cacheable(value = "users", key = "#id") },
    put = { @CachePut(value = "users", key = "#result.email") }
)
```

### 🚀 运行方式
```bash
cd spring-boot-examples
./run-app.sh cache
```

### 🧪 测试用例

#### 1. 第一次查询用户（慢，2秒延迟）
```bash
curl http://localhost:8080/api/users/1
# 响应：{"user":{...},"duration":"2003ms","cached":false}
```

#### 2. 第二次查询用户（快，从缓存读取）
```bash
curl http://localhost:8080/api/users/1
# 响应：{"user":{...},"duration":"5ms","cached":true}
```

#### 3. 更新用户（更新缓存）
```bash
curl -X PUT http://localhost:8080/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name":"张三（已更新）","email":"zhangsan@example.com","age":26}'
```

#### 4. 删除用户（清除缓存）
```bash
curl -X DELETE http://localhost:8080/api/users/1
```

#### 5. 查看缓存状态
```bash
curl http://localhost:8080/api/cache/stats
```

#### 6. 清空所有缓存
```bash
curl -X DELETE http://localhost:8080/api/cache/all
```

### 💡 学习要点

1. **缓存策略**：
   - Cache-Aside（旁路缓存）：应用直接管理缓存
   - Write-Through（写穿）：更新时同时更新缓存
   - Write-Behind（写后）：异步更新缓存

2. **缓存Key设计**：
   - 使用SpEL表达式：`#id`、`#user.id`、`#result.email`
   - 确保Key唯一性，避免冲突

3. **缓存一致性**：
   - 更新操作用@CachePut同时更新缓存
   - 删除操作用@CacheEvict清除缓存
   - 避免缓存数据与数据库不一致

4. **性能优化**：
   - 只缓存频繁访问的数据
   - 设置合理的过期时间
   - 监控缓存命中率

5. **注意事项**：
   - 同一类内部调用不会触发缓存（AOP限制）
   - 生产环境使用Redis等分布式缓存
   - 避免缓存穿透、击穿、雪崩

### 🔗 关联前面的学习
- [CacheDemo.java](../../src/advanced/CacheDemo.java) - LRU缓存算法实现
- [OptionalDemo.java](../../src/advanced/OptionalDemo.java) - 处理null值
- [ConcurrencyDemo.java](../../src/advanced/ConcurrencyDemo.java) - 线程安全

---

## 1️⃣1️⃣ ScheduledApplication - 定时任务

### 📝 学习目标
- 理解Spring定时任务机制
- 掌握@Scheduled注解的使用
- 学习Cron表达式
- 了解定时任务配置和最佳实践

### 🔑 核心知识点

#### 1. 启用定时任务
```java
@SpringBootApplication
@EnableScheduling  // 启用定时任务支持
public class ScheduledApplication { }
```

#### 2. 固定频率执行（fixedRate）
```java
@Scheduled(fixedRate = 5000)  // 每5秒执行一次
public void taskWithFixedRate() {
    // 从上一次任务开始时间计算
}
```

#### 3. 固定延迟执行（fixedDelay）
```java
@Scheduled(fixedDelay = 8000)  // 延迟8秒执行
public void taskWithFixedDelay() {
    // 从上一次任务完成时间计算
}
```

#### 4. 初始延迟（initialDelay）
```java
@Scheduled(initialDelay = 10000, fixedRate = 10000)
public void taskWithInitialDelay() {
    // 启动后延迟10秒首次执行
}
```

#### 5. Cron表达式
```java
@Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨2点
public void dailyTask() { }

@Scheduled(cron = "0 0/5 * * * ?")  // 每5分钟
public void everyFiveMinutes() { }

@Scheduled(cron = "0 0 9 * * MON-FRI")  // 工作日9点
public void weekdayMorning() { }
```

#### 6. Cron表达式格式
```
秒 分 时 日 月 周 [年]
0-59 0-59 0-23 1-31 1-12 1-7 (可选)

特殊字符：
*  : 所有值
?  : 不指定值（日和周互斥）
-  : 范围（1-5）
,  : 列举（1,3,5）
/  : 步长（0/15表示从0开始每15秒）
L  : 最后（月的最后一天）
W  : 工作日
#  : 第几个（6#3表示第3个周六）
```

#### 7. 配置线程池
```java
@Bean
public TaskScheduler taskScheduler() {
    ThreadPoolTaskScheduler scheduler = new ThreadPoolTaskScheduler();
    scheduler.setPoolSize(10);  // 线程池大小
    scheduler.setThreadNamePrefix("scheduled-task-");
    return scheduler;
}
```

#### 8. 动态任务
```java
private ScheduledFuture<?> future;

public void startTask() {
    future = taskScheduler.schedule(
        this::executeTask,
        new CronTrigger("0/15 * * * * ?")
    );
}

public void stopTask() {
    if (future != null) {
        future.cancel(false);
    }
}
```

### 🚀 运行方式
```bash
cd spring-boot-examples
./run-app.sh scheduled
```

### 🧪 测试用例

#### 1. 观察定时任务自动执行
启动应用后，观察控制台输出，会看到各种定时任务按设定周期执行。

#### 2. 查看任务执行日志
```bash
curl http://localhost:8080/api/tasks/logs
```

#### 3. 查看任务状态
```bash
curl http://localhost:8080/api/tasks/status
```

#### 4. 手动触发任务
```bash
curl -X POST http://localhost:8080/api/tasks/trigger
```

#### 5. 启动动态任务
```bash
curl -X POST http://localhost:8080/api/tasks/dynamic/start
```

#### 6. 停止动态任务
```bash
curl -X POST http://localhost:8080/api/tasks/dynamic/stop
```

#### 7. 清空日志
```bash
curl -X POST http://localhost:8080/api/tasks/logs/clear
```

### 💡 学习要点

1. **fixedRate vs fixedDelay**：
   - fixedRate：固定频率，从开始时间算
   - fixedDelay：固定延迟，从结束时间算
   - 长时间任务建议用fixedDelay

2. **常用Cron示例**：
   - `0 0 2 * * ?` - 每天凌晨2点
   - `0 0/5 * * * ?` - 每5分钟
   - `0 0 9 * * MON-FRI` - 工作日上午9点
   - `0 0 0 1 * ?` - 每月1号凌晨
   - `0 0 8 ? * MON` - 每周一上午8点

3. **线程池配置**：
   - 默认单线程，多任务会排队
   - 配置线程池提高并发能力
   - 设置合理的线程数

4. **动态任务**：
   - 使用TaskScheduler手动调度
   - 支持运行时启动/停止
   - 适合需要动态控制的场景

5. **注意事项**：
   - 避免在定时任务中进行耗时操作
   - 注意任务执行时间和周期的关系
   - Cron表达式要仔细测试
   - 生产环境考虑分布式任务调度（XXL-JOB、Elastic-Job）
   - 做好任务监控和日志记录

### 🔗 关联前面的学习
- [ScheduledTaskDemo.java](../../src/advanced/ScheduledTaskDemo.java) - Java定时任务基础
- [ThreadDemo.java](../../src/advanced/ThreadDemo.java) - 多线程基础
- [AsyncDemo.java](../../src/advanced/AsyncDemo.java) - 异步编程

---

## 📊 三个示例对比

| 特性 | Transaction | Cache | Scheduled |
|-----|------------|-------|-----------|
| **难度** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **使用场景** | 数据一致性 | 性能优化 | 定时任务 |
| **核心注解** | @Transactional | @Cacheable/@CachePut/@CacheEvict | @Scheduled |
| **配置复杂度** | 中等 | 中等 | 简单 |
| **生产环境** | 必须 | 推荐 | 常用 |

---

## 🎯 学习建议

### 第1天：事务管理
1. 理解ACID特性和事务传播行为
2. 运行示例，测试转账成功和失败场景
3. 观察事务回滚效果
4. 尝试修改传播行为，理解REQUIRED和REQUIRES_NEW的区别

### 第2天：缓存应用
1. 理解缓存的作用和使用场景
2. 运行示例，对比有无缓存的查询速度
3. 测试缓存更新和删除操作
4. 查看缓存状态，理解缓存管理

### 第3天：定时任务
1. 理解fixedRate和fixedDelay的区别
2. 学习Cron表达式语法
3. 运行示例，观察不同类型任务的执行
4. 尝试编写自己的定时任务

### 第4天：综合实战
结合三个示例，开发一个完整功能：
- 定时任务从外部API获取数据
- 将数据缓存起来
- 使用事务保存到数据库

---

## 🚀 下一步学习方向

1. **Spring Security** - 安全认证和授权
2. **WebSocket** - 实时通信
3. **Spring Boot Test** - 单元测试和集成测试
4. **Spring Boot Actuator** - 应用监控
5. **微服务架构** - Spring Cloud
6. **消息队列** - RabbitMQ/Kafka集成
7. **分布式缓存** - Redis集成

---

## 📚 推荐资源

### 官方文档
- [Spring Transaction Management](https://docs.spring.io/spring-framework/reference/data-access/transaction.html)
- [Spring Cache Abstraction](https://docs.spring.io/spring-framework/reference/integration/cache.html)
- [Spring Task Scheduling](https://docs.spring.io/spring-framework/reference/integration/scheduling.html)

### 在线教程
- Spring Boot官方指南
- Baeldung Spring教程
- 廖雪峰Spring Boot教程

### 实践建议
1. 动手实践每个示例
2. 修改代码，观察效果
3. 阅读源码，理解原理
4. 结合实际项目应用

---

**祝你学习愉快！现在你已经掌握了Spring Boot开发的核心技能！** 🎉
