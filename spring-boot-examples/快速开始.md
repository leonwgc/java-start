# Spring Boot 快速开始指南

## 🚀 5分钟快速体验

### 第1步：进入项目目录

```bash
cd spring-boot-examples
```

### 第2步：选择一个示例启动

#### 选项1：快速入门（推荐首次运行）

```bash
./run-app.sh quickstart
```

等待应用启动后，打开浏览器访问：
- http://localhost:8080/hello
- http://localhost:8080/greet?name=张三
- http://localhost:8080/info

#### 选项2：RESTful API

```bash
./run-app.sh restful
```

使用curl测试：
```bash
# 获取所有用户
curl http://localhost:8080/api/users

# 获取指定用户
curl http://localhost:8080/api/users/1

# 创建新用户
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{"name":"新用户","email":"test@example.com","age":25}'
```

#### 选项3：Spring Data JPA

```bash
./run-app.sh jpa
```

应用启动后：
1. 查看控制台输出，观察JPA操作过程
2. 访问H2数据库控制台：http://localhost:8080/h2-console
   - JDBC URL: `jdbc:h2:mem:testdb`
   - Username: `sa`
   - Password: (留空)

### 停止应用

在运行应用的终端按 `Ctrl + C`

---

## 💻 使用IDE运行

### VS Code

1. 打开 `spring-boot-examples` 文件夹
2. 找到任意一个Application类（例如：`QuickStartApplication.java`）
3. 点击 `main` 方法上方的 ▶️ 运行按钮
4. 或者右键 → Run Java

### IntelliJ IDEA

1. 导入 `spring-boot-examples` 为Maven项目
2. 等待依赖下载完成
3. 找到任意一个Application类
4. 右键类名 → Run 'XxxApplication'

---

## 🔧 Maven命令行运行

### 方法1：使用spring-boot插件

```bash
# 快速入门
mvn spring-boot:run -Dstart-class=com.example.quickstart.QuickStartApplication

# RESTful API
mvn spring-boot:run -Dstart-class=com.example.restful.RestfulApplication

# Spring Data JPA
mvn spring-boot:run -Dstart-class=com.example.jpa.JpaApplication
```

### 方法2：打包后运行

```bash
# 打包
mvn clean package

# 运行
java -jar target/spring-boot-examples-1.0.0.jar
```

---

## 📝 测试API的工具

### curl（命令行）

```bash
# GET请求
curl http://localhost:8080/api/users

# POST请求
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{"name":"测试","email":"test@example.com","age":25}'
```

### Postman（图形界面）

1. 下载Postman：https://www.postman.com/downloads/
2. 导入以下请求：
   - GET http://localhost:8080/api/users
   - POST http://localhost:8080/api/users
   - GET http://localhost:8080/api/users/1
   - PUT http://localhost:8080/api/users/1
   - DELETE http://localhost:8080/api/users/1

### VS Code REST Client插件

创建 `test.http` 文件：
```http
### 获取所有用户
GET http://localhost:8080/api/users

### 获取指定用户
GET http://localhost:8080/api/users/1

### 创建用户
POST http://localhost:8080/api/users
Content-Type: application/json

{
  "name": "测试用户",
  "email": "test@example.com",
  "age": 25
}
```

---

## 🐛 常见问题

### 1. 端口8080已被占用

**错误信息**：
```
Port 8080 was already in use
```

**解决方法**：
修改 `src/main/resources/application.yml`：
```yaml
server:
  port: 8081  # 改为其他端口
```

### 2. Maven依赖下载慢

**解决方法**：
配置阿里云镜像（`~/.m2/settings.xml`）：
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### 3. 找不到主类

**错误信息**：
```
Could not find or load main class
```

**解决方法**：
确保类名正确，包路径完整：
```bash
mvn spring-boot:run -Dstart-class=com.example.quickstart.QuickStartApplication
```

### 4. H2控制台无法访问

**解决方法**：
确保 `application.yml` 配置正确：
```yaml
spring:
  h2:
    console:
      enabled: true
      path: /h2-console
```

重启应用后访问：http://localhost:8080/h2-console

---

## 📚 下一步学习

完成快速体验后，建议：

1. **阅读详细文档**：[SpringBoot学习路径.md](../SpringBoot学习路径.md)
2. **修改代码实验**：尝试添加新接口、新字段
3. **查看源码**：理解每个注解的作用
4. **对比前置示例**：理解Spring Boot如何简化开发

---

## 🎯 学习目标检查

完成快速体验后，你应该能够：

- [ ] 成功启动Spring Boot应用
- [ ] 访问REST接口并查看JSON响应
- [ ] 使用curl或Postman测试API
- [ ] 理解@SpringBootApplication的作用
- [ ] 查看H2数据库的数据
- [ ] 修改代码并重启应用

**完成这些后，继续深入学习每个示例的详细内容！** 🎊

---

## 💡 提示

- 📖 所有代码都有详细的中文注释
- 🔍 查看控制台输出，了解应用运行过程
- 🧪 尝试修改代码，观察变化
- 📝 记录学习笔记
- ❓ 遇到问题先查看文档，再搜索或提问

**祝你学习愉快！** 🚀
