# 2026-02-13 新增示例总结

## 📊 新增概览

本次共新增 **9个高质量学习示例**，涵盖Java现代特性和高级编程技巧。

项目总示例数：**35 → 44个**

---

## ✨ 新增示例详情

### 1️⃣ Java现代特性（Java 14-17+）- 5个

#### 1. RecordsDemo.java - 记录类
**核心内容**：
- Records的定义和使用（Java 14+）
- 紧凑构造器和自定义方法
- Records vs 传统类对比
- DTO和不可变对象最佳实践

**关键代码**：
```java
record Person(String name, int age) {}  // 自动生成构造器、getter、equals等
```

**应用场景**：
- DTO（数据传输对象）
- API请求/响应对象
- 事件对象
- 配置属性类

---

#### 2. SwitchExpressionDemo.java - Switch表达式
**核心内容**：
- 传统switch vs 新switch表达式
- 箭头语法（->）使用
- yield关键字返回值
- 穷尽性检查

**关键代码**：
```java
String result = switch (day) {
    case MONDAY, FRIDAY -> "工作日";
    case SATURDAY, SUNDAY -> "周末";
    default -> "其他";
};
```

**应用场景**：
- 状态机逻辑
- 请求路由
- 错误代码映射
- 配置值转换

---

#### 3. TextBlocksDemo.java - 文本块
**核心内容**：
- 三重引号（"""）语法
- 自动处理换行和缩进
- SQL、JSON、HTML等多行文本
- 格式化方法和转义序列

**关键代码**：
```java
String json = """
    {
        "name": "张三",
        "age": 25
    }
    """;
```

**应用场景**：
- SQL查询语句
- JSON/XML配置
- HTML模板
- 多行错误消息

---

#### 4. VarDemo.java - 类型推断
**核心内容**：
- var关键字使用（Java 10+）
- 局部变量类型推断规则
- var最佳实践和限制
- 提高代码可读性

**关键代码**：
```java
var list = new ArrayList<String>();  // 推断为ArrayList<String>
var map = Map.of("key", "value");    // 推断为Map<String, String>
```

**应用场景**：
- 简化复杂泛型声明
- Lambda表达式变量
- Stream链式调用
- 构建器模式

---

#### 5. SealedClassesDemo.java - 密封类 ⭐⭐⭐
**核心内容**：
- sealed、permits、non-sealed关键字
- 限制继承层次结构
- 与模式匹配结合
- 领域建模应用

**关键代码**：
```java
sealed interface Shape permits Circle, Rectangle {
    double area();
}

final class Circle implements Shape { ... }
```

**应用场景**：
- 领域事件建模
- 状态模式实现
- API响应类型定义
- 异常层次结构

---

### 2️⃣ 高级编程技巧 - 4个

#### 6. FunctionalInterfacesDemo.java - 函数式接口详解 ⭐⭐⭐
**核心内容**：
- Java内置函数式接口（Function、Predicate、Consumer、Supplier）
- 方法引用和构造器引用
- 函数组合（andThen、compose）
- 自定义函数式接口

**关键代码**：
```java
Function<String, Integer> stringLength = String::length;
Predicate<Integer> isEven = x -> x % 2 == 0;
Consumer<String> print = System.out::println;
Supplier<List<String>> listFactory = ArrayList::new;
```

**应用场景**：
- Stream API操作
- 异步回调处理
- 策略模式实现
- 事件监听器

---

#### 7. CollectionsAdvancedDemo.java - 集合高级操作 ⭐⭐⭐
**核心内容**：
- Collectors高级收集器（groupingBy、partitioningBy）
- 分组、分区、聚合统计
- 比较器链式调用
- 不可变集合和并发集合

**关键代码**：
```java
// 按城市分组并计算平均工资
Map<String, Double> avgSalaryByCity = people.stream()
    .collect(Collectors.groupingBy(
        Person::getCity,
        Collectors.averagingDouble(Person::getSalary)
    ));
```

**应用场景**：
- 数据分组统计
- 批量数据处理
- 报表生成
- 数据转换管道

---

#### 8. ConcurrencyDemo.java - 并发工具详解 ⭐⭐⭐⭐
**核心内容**：
- 线程池（ExecutorService、ThreadPoolExecutor）
- 原子类（AtomicInteger、AtomicReference）
- 显式锁（ReentrantLock、ReadWriteLock）
- 同步工具（CountDownLatch、CyclicBarrier、Semaphore）
- 阻塞队列和ForkJoinPool

**关键代码**：
```java
ExecutorService executor = Executors.newFixedThreadPool(5);
AtomicInteger counter = new AtomicInteger(0);
CountDownLatch latch = new CountDownLatch(10);
```

**应用场景**：
- @Async异步执行底层
- 定时任务线程池
- 缓存并发访问
- 高并发场景

---

#### 9. ValidationDemo.java - Bean Validation ⭐⭐⭐
**核心内容**：
- JSR-380验证规范
- 常用验证注解（@NotNull、@Size、@Email等）
- 自定义验证器
- 验证组和级联验证

**关键代码**：
```java
public class User {
    @NotBlank(message = "用户名不能为空")
    private String username;

    @Email(message = "邮箱格式不正确")
    private String email;

    @Min(value = 18, message = "年龄必须大于18岁")
    private int age;
}
```

**应用场景**：
- REST API请求参数验证
- Spring @Valid/@Validated
- 表单数据验证
- 统一异常处理

---

## 📈 学习价值

### 现代Java特性（Records、Switch表达式、文本块等）
- ✅ 代码更简洁易读
- ✅ 减少样板代码
- ✅ 提高开发效率
- ✅ 与Spring Boot 3.x紧密结合

### 高级编程技巧
- ✅ 掌握函数式编程范式
- ✅ 理解并发编程核心工具
- ✅ 学会数据处理最佳实践
- ✅ 为Spring框架学习打下坚实基础

---

## 🎯 学习建议

### 优先级排序

**必学（⭐⭐⭐⭐⭐）**：
1. FunctionalInterfacesDemo - 函数式编程基础
2. SealedClassesDemo - 现代类型系统
3. ConcurrencyDemo - 并发编程核心

**推荐（⭐⭐⭐⭐）**：
4. CollectionsAdvancedDemo - 数据处理必备
5. RecordsDemo - 简化DTO编写
6. ValidationDemo - API验证标准

**增强（⭐⭐⭐）**：
7. SwitchExpressionDemo - 代码优化技巧
8. TextBlocksDemo - 多行文本处理
9. VarDemo - 类型推断应用

---

## 🚀 下一步计划

- [ ] 添加更多Spring Boot实战示例
- [ ] 完善JavaDoc文档
- [ ] 添加单元测试覆盖
- [ ] 创建学习路径图

---

## 📝 测试验证

所有示例已通过编译测试：

```bash
# 编译所有新示例
javac -d target/classes -sourcepath src \
  src/advanced/SealedClassesDemo.java \
  src/advanced/FunctionalInterfacesDemo.java \
  src/advanced/ConcurrencyDemo.java \
  src/collections/CollectionsAdvancedDemo.java \
  src/advanced/RecordsDemo.java \
  src/advanced/SwitchExpressionDemo.java \
  src/advanced/TextBlocksDemo.java \
  src/advanced/VarDemo.java \
  src/advanced/ValidationDemo.java
```

运行示例：
```bash
java -cp target/classes advanced.FunctionalInterfacesDemo
java -cp target/classes advanced.SealedClassesDemo
java -cp target/classes collections.CollectionsAdvancedDemo
```

---

**更新时间**：2026年2月13日
**项目状态**：持续更新中 🚀
